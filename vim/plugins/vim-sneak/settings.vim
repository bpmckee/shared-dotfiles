" Move to the next match immedietly after invoking Sneak by hitting `s` again.
let g:sneak#s_next = 1

" Behave like easymotion.
let g:sneak#label = 1

" Case sensitivity determined by smartcase / ignorecase settings.
let g:sneak#use_ic_scs = 1
